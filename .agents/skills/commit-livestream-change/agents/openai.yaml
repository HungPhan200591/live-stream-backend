interface:
  display_name: "Fast Commit Livestream Change"
  short_description: "Commit with a low-cost model and minimal context"
  default_prompt: "Use $commit-livestream-change to commit all current local changes quickly without pushing."
