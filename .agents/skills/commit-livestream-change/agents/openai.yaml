interface:
  display_name: "Commit Livestream Change"
  short_description: "Commit all local changes safely without pushing"
  default_prompt: "Use $commit-livestream-change to commit all current local changes in one commit without pushing."
