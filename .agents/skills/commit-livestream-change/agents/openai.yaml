interface:
  display_name: "Fast Local Commit"
  short_description: "Fast low-cost local commit without push"
  default_prompt: "Use $commit-livestream-change to commit all local changes quickly without pushing."
